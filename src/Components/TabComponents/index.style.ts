import {StyleSheet} from 'react-native';
import COLORS from '../../UI/colors';
export default StyleSheet.create({
  label: {
    textAlign: 'center',
    alignItems: 'center',
    color: COLORS.navy,
    paddingTop: 1,
    marginBottom: 2,
  },
});
